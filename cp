cp makes copies of files and directories.

For example:
cp file.txt filecopy.txt

Files can also be copied into different directories if you include the path to the files.

For example:

cp ~/projects/blue/file.txt ~/projects/red/file.txt
